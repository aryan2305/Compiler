#include <stdio.h>

int x;
int y;

int main()
{
    int n, m=0;
    get(n);
    
    while (n >= 0 && m>=0)
    {
        for(i=0;i<n;i=i+3)
        {
            put(i);
        }
        put(n);
        n = n - 1;
        m=(m*2/(1+2*3));
        switch (n)
        {
            case 1:
                if(n>0)
                {

                }
                break;
            case 2:
                if(n>0)
                {

                }
                break;

        }
        if(((n&&m))||(!i))
        {
            put(1);
        }
        else
        {
            
        }
        
    }

    int n = 1;
    
}